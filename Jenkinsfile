pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Example: Run a shell command
                sh 'echo "Building..."'
            }
        }
        stage('Test') {
            steps {
                // Example: Run tests
                sh 'echo "Testing..."'
            }
        }
        stage('Deploy') {
            steps {
                // Example: Deployment steps
                sh 'echo "Deploying..."'
            }
        }
    }
}
